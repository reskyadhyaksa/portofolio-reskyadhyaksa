import NavigationBar from "../component/navbar";

export default function ProjectPage() {

  return(
    <div className="w-full min-h-screen">
      <NavigationBar pathname={'/projects'}/>
    </div>
  )
}